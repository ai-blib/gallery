import styled from "styled-components";

export namespace AccordionPanelStyle {
    export const Wrap = styled('div')`
      width: 100%;
      height: auto;

      .d-title {
        font-family: 'Inter';
        font-style: normal;
        font-weight: 700;
        font-size: 16px;
        line-height: 160%;
        /* identical to box height, or 26px */
        text-align: center;
        /* Text/main */
        color: rgba(0, 0, 0, 0.88);
        margin: 0px 8px;
      }
    `
}